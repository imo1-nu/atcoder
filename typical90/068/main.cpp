#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using ll = long long;

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    int N, Q;
    cin >> N >> Q;

    for (int i = 0; i < Q; i++) {
        int t, x, y, v;
        cin >> t >> x >> y >> v;
        x--; y--;
        if (t == 0) {

        }
        else {

        }
    }
}
