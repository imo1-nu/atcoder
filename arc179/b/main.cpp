#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll w = 998244353;

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    ll m, n;
    cin >> m >> n;
    vector<ll> x(m);
    for (ll i = 0; i < m; i++) {
        cin >> x[i];
    }
    
}
