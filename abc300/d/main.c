#include <stdio.h>

int prime(int *p, long long n)
{
    int i, s;
    p[0] = 2;
    for (i = 0; i * i <= n / 12; i++) {
        for (s = 1; s <= i; j++) {
        }
    }
}

int main()
{
    long long n;
    scanf("%lld" &n);

    int a, b, c, p[100000];
    prime(p, n);
}