#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N, M, ans = 1;
    string S, T;
    cin >> N >> M >> S >> T;

    if (S[0] != T[0]) ans = 0;
    else {
        for (int i = 0; i < N - 1; i++) {
            if (S[i] != T[M - 1] && S[i + 1] != S[0] && S[i + 1] !=) {
            }
        }
    }
}
