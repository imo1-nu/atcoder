#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using ll = long long;

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    string S;
    cin >> S;
    vector<vector<int>> A(26);
    for (int i = 0; i < S.length(); i++) {
        A[S[i] - 'A']
    }
    
}
