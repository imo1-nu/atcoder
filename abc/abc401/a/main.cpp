#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using ll = long long;

int main() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    int S;
    cin >> S;
    if (200 <= S && S < 300) cout << "Success" << endl;
    else cout << "Failure" << endl;
}
