#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using ll = long long;

int main() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    char c;
    cin >> c;
    if (c == 'B') cout << 'Y' << endl;
    if (c == 'Y') cout << 'R' << endl;
    if (c == 'R') cout << 'B' << endl;
}
